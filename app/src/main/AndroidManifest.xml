<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ireribrian.mysqlapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.Register" />
        <activity android:name=".activities.Home" />
        <activity android:name=".activities.Layouts" />
        <activity android:name=".activities.UpdateProfile" />
        <activity android:name=".ui.CvProfile" />
        <activity android:name=".ui.SocialMediaOne" />
        <activity android:name=".ui.SocialMediaTwo" />
        <activity android:name=".ui.CvProfileTwo"></activity>
    </application>

</manifest>